<UserControl x:Class="NetworkService.Views.DragDropCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetworkService.Views"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    <Border BorderBrush="Black" BorderThickness="1" Background="Black" CornerRadius="10">
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Source="{Binding ServerType.ServerTypeImage}" Width="50" Height="50" />
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding Name}" Style="{StaticResource font_style}"/>
                <TextBlock Text="{Binding IpAddress}" Margin="0,5,0,0" Style="{StaticResource font_style}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
